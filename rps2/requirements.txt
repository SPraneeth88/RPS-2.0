# RPS 2.0 — runtime dependencies
# Pinned to floors that match the versions this prototype was built and tested on.

# Agentic workflow engine
langgraph>=0.2.0
langchain-core>=0.3.0

# Web / API layer
fastapi>=0.115.0
uvicorn[standard]>=0.30.0

# Data layer
SQLAlchemy>=2.0.0

# Validation / schemas
pydantic>=2.7.0

# Optional: LLM-backed NLU (the app runs fully without this — falls back to
# a deterministic rule-based parser when ANTHROPIC_API_KEY is unset).
anthropic>=0.40.0

# Config
python-dotenv>=1.0.0

# --- dev / test only ---
pytest>=8.0.0
httpx>=0.27.0
